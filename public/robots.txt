# ------------------------------
# Robots.txt for rami.page.gd
# ------------------------------

# Allow all bots to access everything
User-agent: *
Disallow: /cgi-bin/
Disallow: /tmp/
Disallow: /private/

# Prevent crawling of unnecessary query URLs (if any)
Disallow: /*?*

# Sitemap location
Sitemap: https://rami.page.gd/public/sitemap.xml

# Optional: Block specific bots (example)
# User-agent: BadBot
# Disallow: /
